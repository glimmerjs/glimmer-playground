<glimmer-visualizer>
  {{#each opcodes key="@index" as |op|}}
    <div class="op">
      <span class="opcode">{{hex op.opcode}}</span>
      <span class="name">{{op.name}}</span>
      {{#if op.operands}}
        <div class="operands">
          {{#each op.operands key="@index" as |operand|}}
            <div class="operand {{operand.type}}">
             {{#if operand.name}}
               <span class="name">
                 {{operand.name}}:
               </span>
              {{/if}}
              <span class="value">
                {{operand.value}}
              </span>
              <span class="raw-value">
                ({{hex operand.rawValue}})
              </span>
            </div>
          {{/each}}
        </div>
      {{/if}}
    </div>
  {{/each}}
</glimmer-visualizer>